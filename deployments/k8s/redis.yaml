apiVersion: apps/v1
kind: Deployment
metadata:
  name: niceagent-redis
  namespace: niceagent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: niceagent-redis
  template:
    metadata:
      labels:
        app: niceagent-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: niceagent-redis
  namespace: niceagent
spec:
  type: ClusterIP
  selector:
    app: niceagent-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
